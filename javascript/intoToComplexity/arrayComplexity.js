// Arrays: Ordered Lists

// Array Complexities
// Accesss - O(1)
// Searching - O(n)
// Insertion/Removal - depends - push/pop Operations better than shift/unshift
// Insertion/Removal - add/remove value at the end of array is O(1) 
// Insertion/Removal - add/remove value at the start of array is O(n)

let names = ['Tushar', 'Andrew', 'Colt', 'Ronaldo']

let values = [true, {}, [], 2, "awesome"]

// Array Operation Complexities
// push - O(1)
// pop - O(1)
// shift - O(n)
// unshift - O(n)
// concat - O(n)
// slice - o(n)
// splice - O(n)
// sort - O(n * log(n))
// forEach/map/filter/reduce/etc - O(n)
